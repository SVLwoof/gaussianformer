# Path to your cluster's lmod init script (sourced before `module load` in runs/*.sh).
# Default: /etc/profile.d/lmod.sh
LMOD_INIT=/etc/profile.d/lmod.sh

# Override where data_external/build_tomatoes_strip.py writes the strip PNGs.
# Default: <repo>/figures
# REPORT_FIGURES_DIR=/path/to/report/figures
